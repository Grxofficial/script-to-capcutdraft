# 参与贡献

感谢你改进 Script to CapCut Draft。

## 开发要求

- 使用 Python 3.11。
- 不提交真实 API Key、用户素材、语音、任务缓存或剪映注册表。
- 不提交没有公开授权的文案和媒体样本。
- 跨平台改动必须说明实际在哪个操作系统、哪个剪映版本验证。
- 修改草稿安装逻辑时，不得绕过版本冒烟、用户确认、备份和回滚。
- 从上游项目复制或实质性改写代码时，保留原归属并在修改文件中显著说明变更。

## 验证

```bash
python -m unittest discover -s tests -v
python -m compileall -q src tests
```

涉及真实剪映安装的改动还需要人工确认：草稿能打开、视频/旁白/字幕三轨存在、媒体不丢失且字幕可编辑。
