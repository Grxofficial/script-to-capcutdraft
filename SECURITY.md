# 安全策略

## 报告安全问题

请不要在公开 Issue、日志或截图中提交 API Key、用户素材、剪映注册表或设备信息。仓库上线后，请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下报告安全问题。

## 本地安全边界

- API Key 只保存在被 Git 忽略的 `.env` 中。
- 网页控制台只监听 `127.0.0.1`，没有为公网部署设计认证系统。
- 项目不会读取或尝试解密已有加密剪映草稿。
- 安装草稿前会备份注册信息，但重要剪映工程仍应由用户单独备份。
- `smoke approve --confirmed` 必须由用户完成真实人工检查，Agent 不能代替确认。

## 支持范围

剪映草稿格式可能随客户端版本变化。未通过当前“操作系统 + 剪映版本”冒烟门禁时，项目只生成 staging，不应强行覆盖正式草稿库。
